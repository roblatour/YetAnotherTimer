<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude Jetpack DataStore preferences (stored under files/datastore) so uninstall truly clears settings -->
    <exclude domain="file" path="datastore/settings.preferences_pb" />
    <!-- Defensive: exclude entire DataStore directory -->
    <exclude domain="file" path="datastore/" />
</full-backup-content>
